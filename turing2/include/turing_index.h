#ifndef TURING_INDEX_H
#define TURING_INDEX_H

#include <gmp.h>
#include <stdbool.h>

// typedef struct
// {
//     mpz_t indexMpz;
// } tm_index_t;

// void turing_index_init(tm_index_t* index);
// void turing_index_destroy(tm_index_t* index);

// // (int)index1;
// int turing_index_cast_int(tm_index_t* index1);

// // index1 = i;
// void turing_index_set_int(tm_index_t* index1, int i);

// // index1 += i;
// void turing_index_add_int(tm_index_t* index1, int i);

// // index1 += index2;
// void turing_index_add_index(tm_index_t* index1, tm_index_t* index2);

// // index1 -= index2;
// void turing_index_sub_index(tm_index_t* index1, tm_index_t* index2);

// // index1 /= index2;
// void turing_index_div_index(tm_index_t* index1, tm_index_t* index2);

// // index1 *= index2;
// void turing_index_mul_index(tm_index_t* index1, tm_index_t* index2);

// // index1 <= index2
// bool turing_index_le_index(tm_index_t* index1, tm_index_t* index2);

// // prints index1 into buffer null-terminted.
// int turing_index_toCharArray(char* buffer, tm_index_t* index1);

// // loads index from null-terminated buffer
// int turing_index_fromCharArray(char* buffer, tm_index_t* index1);

#endif